<!DOCTYPE html>

<html>

<body>

        <div>
            {{ message }}

        </div>
        <div>
            <h3 class="infolabel" for="title">title  </h3>
            <p id="title">{{ movie.title }}</p>
        </div>
        <div>
            <a href="{{ asset('detail/',{'id' : movie.id})}}">
                <img id="movie_detail_img" src="{{ asset('img/posters/')}}{{ movie.image }}">
            </a>
        </div>

</body>
</html>
